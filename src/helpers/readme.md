# The matches generator is file scheduler.js

## To run the generator. 
Open a new terminal window
Go to the bfbh-cup folder

run 'node scheduler'

If folder 'matches_generator' exists, than a new file will generate with the automated matchups. 
The file structure is matches-1, matches-2, etc. 
